<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8">
    <style>
        body {
            margin: 0;
            padding: 15mm 20mm 0 20mm;
            font-family: 'Roboto', 'Helvetica Neue', Arial, sans-serif;
            font-size: 11px;
            color: #828384;
            width: 100%;
            box-sizing: border-box;
            -webkit-print-color-adjust: exact;
            /* Debug border removed after verification */
        }
        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: flex-end;
            border-bottom: 2px solid #e96354;
            padding-bottom: 8px;
            width: 100%;
            box-sizing: border-box;
        }
        .logo {
            font-size: 16px;
            font-weight: 700;
            color: #e96354;
        }
        .logo span {
            color: #494e50;
        }
        .claim {
            font-style: italic;
            font-size: 10px;
        }
    </style>
</head>
<body>
    <div class="header-container">
        <div class="logo">focus<span>shop</span></div>
        <div class="claim">{{ "TopdataPdfDatasheetSW6.brandClaim"|trans }}</div>
    </div>
</body>
</html>
